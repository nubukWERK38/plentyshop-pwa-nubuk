Formuliere alle Antworten kurz und praegnant. Keine Floskeln oder unnoetige Hoeflichkeiten. Bleib zielorientiert. Effizienz und Genauigkeit sind vordergruendig.

Projektumgebung:

- Codex laeuft hier lokal unter Windows/PowerShell im Workspace `J:\Github\Neuer Ordner\plentyshop-pwa-nubuk`.
- GitHub CLI ist auf Windows installiert unter `C:\Program Files\GitHub CLI\gh.exe`.
- `gh` ist fuer GitHub.com authentifiziert als Account `nubukWERK38`.
- Falls `gh` in einer laufenden Codex-Sitzung nicht im `PATH` gefunden wird, nutze direkt:
  `& "C:\Program Files\GitHub CLI\gh.exe" ...`
- Nach einem Codex-Neustart sollte `gh ...` direkt ueber den PATH funktionieren.
- Fuer GitHub-Aufgaben bevorzugt `gh` verwenden: PRs erstellen/pruefen, Issues lesen/erstellen, Actions/Workflows pruefen oder starten, Repo-Status abfragen.
- Nach Layout-/CSS-/Vue-Aenderungen immer Prettier ueber die geaenderten Dateien laufen lassen.
